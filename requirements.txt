flask>=3.0
python-pptx>=1.0
Pillow>=10.0
opencv-python-headless>=4.8
easyocr>=1.7
moviepy>=2.0
numpy>=1.24

rembg                                # Background removal (~170 MB model)
PyMuPDF                              # PDF→JPG (primary — no external binary needed)
pdf2image                            # PDF→JPG fallback (requires poppler in PATH)

# ── Optional extras (uncomment as needed) ─────────────────────────────────
# authlib                              # Google OAuth login
# google-api-python-client             # Google Slides export
# google-auth                          # Google Slides export
# google-auth-oauthlib                 # Google Slides export bootstrap
# qrcode[pil]                          # QR overlay generation

